import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:firebase_core/firebase_core.dart';

void main() async {
  // This is a dummy script. Real seed should be run in the app since it needs Firebase initialized.
}
